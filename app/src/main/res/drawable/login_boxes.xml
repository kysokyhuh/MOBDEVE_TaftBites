<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/>  <!-- Background color for the box -->
    <stroke
        android:width="2dp"
        android:color="@color/orange" />  <!-- Border thickness and color -->
    <corners android:radius="8dp"/>  <!-- Rounded corners for the box -->
    <padding android:left="10dp" android:right="10dp" />  <!-- Padding inside the box -->
</shape>
